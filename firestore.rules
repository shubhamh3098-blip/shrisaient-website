rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Shri Sai Enterprises - Store collection
    match /stores/{storeId} {
      allow read, write: if true;
    }
  }
}
